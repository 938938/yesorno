import React from 'react';
import styled from 'styled-components';

const ChangeBtn = () => {
  return <Button>ChangeBtn</Button>;
};

export default ChangeBtn;

const Button = styled.button`
  position: absolute;
  top: 10px;
  right: 10px;
`;
